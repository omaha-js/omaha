{
	"name": "@omaha/cli",
	"version": "dev",
	"description": "A command line interface for the Omaha API.",
	"author": "Bailey Herbert <git@bailey.sh> (https://bailey.sh)",
	"repository": "github:baileyherbert/omaha",
	"bugs": "https://github.com/baileyherbert/omaha/issues",
	"license": "MIT",
	"main": "dist/main.js",
	"types": "dist/main.d.ts",
	"files": [
		"dist/**/*"
	],
	"keywords": [],
	"scripts": {
		"dev": "rimraf dist && tsc -w",
		"build": "rimraf dist && tsc",
		"start": "node dist/main",
		"test": "jest"
	},
	"devDependencies": {
		"@types/jest": "^27.4.1",
		"@types/node": "^16.11.7",
		"jest": "^27.5.1",
		"rimraf": "^3.0.2",
		"ts-jest": "^27.1.3",
		"typescript": "^4.6.2"
	},
	"jest": {
		"preset": "ts-jest",
		"testEnvironment": "node",
		"globals": {
			"ts-jest": {
				"tsconfig": "tests/tsconfig.json"
			}
		}
	}
}
