@@ An access token was deleted

Hi {{ account.name }},

We're writing to let you know that an access token has been deleted from the "{{ repository.name }}" repository that you manage. Here are the details:

•  Name: {{ token.name }}
•  Description: {{ token.description ?: "None provided" }}

You can review your access tokens here:
{{ url('/repository', repository.id, 'settings/tokens') }}

Regards,
{{ env.APP_NAME }}
