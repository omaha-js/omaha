@@ A new access token was created for your account

Hi {{ account.name }},

We're writing to let you know that a new access token has been created that grants access to your account. Here are the details:

•  Name: {{ token.name }}
•  Description: {{ token.description ?: "None provided" }}
•  Expiration: {{ token.expires_at.toISOString() ?: "Never" }}

You can review your access tokens here:
{{ url('/account/settings/tokens') }}

Regards,
{{ env.APP_NAME }}
