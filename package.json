{
	"private": true,
	"scripts": {
		"dev": "pnpm -r --parallel run dev",
		"build": "pnpm -r run build"
	},
	"dependencies": {},
	"devDependencies": {},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [
				"webpack",
				"@popperjs/core"
			],
			"allowedVersions": {
				"typeorm": "0.3.5"
			}
		}
	}
}
